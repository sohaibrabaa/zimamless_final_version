[pytest]
# rootdir is services/ml so `from app...` resolves without an installed
# package or a PYTHONPATH incantation in CI.
pythonpath = .
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
